---
---
- if item[:kind] == 'article'
  .meta
    %ul.tags
      %li= pretty_time(item[:created_at])
      - if disqus_shortname 
        - @config[:include_disqus_js] = true
        %li
          &middot;
        %li
          %a(href="http://www.leonardoborges.com/writings#{item.identifier}#disqus_thread")
      - if item[:tags]
        %li &middot;
        - item[:tags].sort.each do |tag|
          %li
            %a(href="/tag/#{tag.downcase.gsub(' ','-')}/" class='tag')
              = tag
