---
---
.excerpt
  - url = "#{route_path(item,false)}"
  %h2 
    %a(href="#{url}" title="Full article" class="permalink")= "&laquo; #{item[:title]}"
  .body
    %p
      = item[:excerpt] || "!!! Missing excerpt metadata for <code>#{item[:file].path}</code> " + item.identifier
  -# = render('_meta', :item => item)
    
